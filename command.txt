CU=$(stat -f%Su /dev/console); U=$(id -u "$CU"); echo "console: $CU uid: $U"; sudo launchctl asuser $U sudo -u "$CU" open -a Safari; sleep 2; sudo launchctl asuser $U sudo -u "$CU" open -a "System Settings"
